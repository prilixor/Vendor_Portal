# --- Local development (recommended) ---
# Leave VITE_API_BASE_URL unset: the app uses same-origin `/api` and Vite proxies to the .NET API.
#
# VITE_DEV_API_PROXY_TARGET=https://localhost:51884
#
# --- Override direct API URL (optional) ---
# VITE_API_BASE_URL=
